{% extends 'base.html.twig' %}

{% block title %}TimetableItem{% endblock %}

{% block body %}
    <h1>TimetableItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ timetable_item.id }}</td>
            </tr>
            <tr>
                <th>DateStart</th>
                <td>{{ timetable_item.dateStart ? timetable_item.dateStart|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateEnd</th>
                <td>{{ timetable_item.dateEnd ? timetable_item.dateEnd|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_timetable_item_index') }}">back to list</a>

    <a href="{{ path('app_timetable_item_edit', {'id': timetable_item.id}) }}">edit</a>

    {{ include('timetable_item/_delete_form.html.twig') }}
{% endblock %}
