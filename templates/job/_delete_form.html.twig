<div class="d-inline-block">
    <a class="btn btn-warning" href="{{ path('app_job_edit', {'id': job.id}) }}">Edit</a>
</div>

<form class="d-inline-block" method="post" action="{{ path('app_job_delete', {'id': job.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
