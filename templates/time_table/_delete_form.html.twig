<div class="d-inline-block">
    <a class="btn btn-warning" href="{{ path('app_time_table_edit', {'id': time_table.id}) }}">Edit</a>
</div>
<form class="d-inline-block" method="post" action="{{ path('app_time_table_delete', {'id': time_table.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ time_table.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
