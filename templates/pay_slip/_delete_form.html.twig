<div class="d-inline-block">
    <a class="btn btn-warning" href="{{ path('app_pay_slip_edit', {'id': pay_slip.id}) }}">Edit</a>
</div>

<form class="d-inline-block" method="post" action="{{ path('app_pay_slip_delete', {'id': pay_slip.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pay_slip.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
